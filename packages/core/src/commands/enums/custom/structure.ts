import { CustomEnum } from ".";

class StructureOperation extends CustomEnum {
  public static readonly identifier = "structure_operation";
  public static readonly options = ["load"];
}

export { StructureOperation };
