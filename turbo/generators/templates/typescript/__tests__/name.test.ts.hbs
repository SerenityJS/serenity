import { getName } from "..";

describe("getName function", () => {
  it("should return '@serenityjs/{{ dashCase name }}'", () => {
    const expectedOutput = "@serenityjs/{{ dashCase name }}";
    const result = getName();
    expect(result).toBe(expectedOutput);
  });
});
