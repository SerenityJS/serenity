import { getName } from "..";

describe("getName function", () => {
  it("should return '@typescript-rust-turbo/{{ dashCase name }}'", () => {
    const expectedOutput = "@typescript-rust-turbo/{{ dashCase name }}";
    const result = getName();
    expect(result).toBe(expectedOutput);
  });
});
